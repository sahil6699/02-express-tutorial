const express = require("express");

const app = express();

app.listen(5000, () => {
  console.log("Server is listening on port ", 5000);
});

/*
  app.get
  app.post
  app.delete
  app.put
  app.delete
  app.all
  app.listen
  app.use
*/
